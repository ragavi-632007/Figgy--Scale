import 'package:flutter/material.dart';

class AppAnimations {
  static const Duration entryDuration = Duration(milliseconds: 800);
  static const Duration staggerInterval = Duration(milliseconds: 600);
  static const Duration disruptionInterval = Duration(milliseconds: 2500);
  static const Curve entryCurve = Curves.elasticOut;
  static const double scaleStart = 0.92;
  static const double slideOffset = 32.0;
}
