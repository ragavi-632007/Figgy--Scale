import 'package:flutter/material.dart';

/// Colors for the embedded Shield / Claims experience (from mudiayalaba).
/// Kept separate from [AppColors] in app_theme.dart to avoid name clashes.
class ShieldColors {
  static const Color primary = Color(0xFFF2994A);
  static const Color secondary = Color(0xFF2D9CDB);
  static const Color background = Color(0xFFFFFFFF);
  static const Color cardBg = Color(0xFFFBFBFB);
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textLight = Color(0xFF757575);
  static const Color success = Color(0xFF27AE60);
  static const Color danger = Color(0xFFEB5757);
  static const Color highlightBg = Color(0xFFFEF5ED);
  static const Color infoBg = Color(0xFFEFF6FF);
}
